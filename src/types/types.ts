export type UserData = {
  name: string;
  age: string;
  email: string;
  gender: string;
  country: string;
  password: string;
  // photo: string;
};

export type ResultsState = {
  data: UserData[];
};
